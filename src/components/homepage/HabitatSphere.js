import React, { Component } from "react";
import * as THREE from "three";

class HabitatSphere extends Component {
    componentDidMount() {
    // === THREE.JS CODE START ===

    // === THREE.JS CODE END ===
  }
  render() {
    return (
        <canvas id="blob"></canvas>
    )
  }
}

export default HabitatSphere
